name: 'Hello World'
description: 'Greet someone'

runs:
  using: "composite"
  steps:
    - name: Set Greeting
      run: echo "PUBLIC ACTION LOG"
      shell: bash
    - name: Sample API
      shell: bash
      run: |
        curl -L \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer mocktokennotreal" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          https://api.github.com/users/bryanaexp
